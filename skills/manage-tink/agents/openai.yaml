interface:
  display_name: "Tink"
  short_description: "Repository-owned Agent Skills via the Tink CLI"
  default_prompt: "Use $manage-tink to set up or manage this project's agent skills with Tink."
